[project]
name = "uptime-api"
version = "0.1.0"
description = "uptime_net control plane API (app package)."
requires-python = ">=3.11"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
